{% if label_hidden %}
  {% for item in items %}
    <div class="border-box -inline">{{ item.content }}</div>
  {% endfor %}
{% else %}
  {{ label }}
  {% for item in items %}
    <div class="border-box -inline">{{ item.content }}</div>
  {% endfor %}
{% endif %}
